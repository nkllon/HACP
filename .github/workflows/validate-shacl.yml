name: Validate SHACL & Examples

on: [push, pull_request]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - name: Install pyshacl
        run: |
          python -m pip install --upgrade pip
          pip install pyshacl rdflib
      - name: Validate examples with SHACL
        run: |
          # For demonstration, assume TTL shapes in shacl/ and examples converted to RDF TTL
          # Users should expand this step to convert YAML->RDF or provide RDF examples.
          echo "Note: Add YAML->RDF conversion or RDF examples to validate."
